#include "binModel.hpp"
#include <vector>

using namespace core;

void binModel::load(const char* path, std::vector<float>& vao, std::vector<unsigned int>& ebo)
{
	bool reading = true;
	while (reading)
	{
		reading = false;
	}
}
